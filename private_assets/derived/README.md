# 本地衍生视觉资产（不提交）

公开版不包含总览板、contact sheet、原像素裁剪参考图或其他由源素材生成的视觉文件。

本地分析时可按以下相对路径放置或生成：

```text
private_assets/derived/overview/
private_assets/derived/contact_sheets/
private_assets/derived/reference_crops/
```

这些目录受根目录 `.gitignore` 保护。
